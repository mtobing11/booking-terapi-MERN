import { createTheme } from '@mui/material/styles';

const changeColorTheme1 = createTheme();

changeColorTheme1.typography.body1 = {
    color: '#EFEFEF',
    fontWeight: '100',
    fontSize: '0.7rem',
}
export default changeColorTheme1